@* define the model of cinema *@
@model Cinema
@* define the viewdata *@
@{
    ViewData["Title"] = "Add New Cinema";
}
@* define the code *@
<section>
    <div class="row mb-5 pb-5">
        <div class="col-md-8 offset-md-2">
            <p>
                <h2>Add New Cinema</h2>
            </p>
            <div class="row">
                <div class="col-md-8 offset-2">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="text-center form-group">
                            <img id="ImagePreview" style="max-width: 200px; max-height: 200px;" />
                        </div>
                        <div class="form-group">
                            <label asp-for="Logo" class="form-label"></label>
                            <input id="ImageUrlInput" asp-for="Logo" class="form-control" />
                            <span asp-validation-for="Logo" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Name" class="form-label"></label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Description" class="form-label"></label>
                            <input asp-for="Description" class="form-control" />
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                        <div class="form-group mt-3">
                            <input type="submit" value="Creat Now" class="btn btn-sm btn-success float-end" />
                            <a asp-action="Index" class="btn btn-sm btn-secondary">
                                Show All
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
@* dfine scripts *@
@section Scripts{
    <script>
        // Get references to the input and img elements
        const imageUrlInput = document.getElementById('ImageUrlInput');
        const imagePreview = document.getElementById('ImagePreview');

        // Attach an 'input' event listener to the input field
        imageUrlInput.addEventListener('input', function () {
            // Get the value from the input field
            const imageUrl = this.value;

            // Set the 'src' attribute of the img tag to the entered URL
            imagePreview.src = imageUrl;
        });
    </script>

}